language: python
python:
  - "3.5"
install:
  - pip install pytest-cov
script:
  - make test
  - py.test rpn.py --doctest-modules --pep8 coveralls -v --cov coveralls --cov-report term-missing
after_success:
  - coveralls